name: database_utils
description: A full static class to manage the database operations in Flutter using Firebase Realtime Database.
repository: https://github.com/MoniKode-Mobile-Solutions/DatabaseUtils
homepage: https://www.monikode.com
version: 0.0.4

environment:
  sdk: '>=3.3.1 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_database: ^10.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true